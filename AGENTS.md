# AGENTS.md

Read the global AGENTS.md first: `~/.agents/AGENTS.md`.

Ship reviewed and tested changes with the `ship-worktree` skill. Do not create
pull requests or push worktree branches. Code shipping does not authorize
publishing binary releases; follow `docs/releases.md` for release preparation.
